<div class="container">
  <h1>Journeys</h1>
  
  <table class="table table-striped">
  <% @journeys.each do |journey| %>
    <tr>
      <td><%= link_to journey, journey %></td>
    </tr>
  <% end %>
  </table>
  
  <%= paginate @journeys %>

</div>
